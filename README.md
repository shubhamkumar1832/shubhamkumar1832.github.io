# Shubham Kumar — liquid-glass portfolio hero

    npm install
    npm run dev          # http://localhost:3000
    npm run typecheck    # tsc --noEmit
    npm run build && npm start

## Images (public/images/)
`Base_image_desktop.png` and `Base_image_mobile.png` are crops of the supplied portrait.
`Reveal_image_desktop.png` and `Reveal_image_mobile.png` are PLACEHOLDERS (blue duotone).
Replace them with your liquid-glass anatomical versions, keeping the same pixel alignment
as their base image (16:9 desktop, 9:16 mobile).

## Tweaks
- Mobile text colour over the portrait: `--mobile-copy` in app/globals.css.
- Nav links (About / Work / Process / Experiments) point at #anchors; there are no sections yet.
